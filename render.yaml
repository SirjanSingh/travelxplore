services:
  - type: web
    name: travelxplore-server
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: CLIENT_URL
        value: https://localxplore.vercel.app
      - key: NODE_ENV
        value: production
